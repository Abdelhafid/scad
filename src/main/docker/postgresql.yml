version: '2'
services:
  scad-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/scad/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=scad
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
